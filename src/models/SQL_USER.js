class SQL_USER {
  constructor(firstname, lastname, email, password, user_id) {
    this.firstname = firstname;
    this.lastname = lastname;
    this.email = email;
    this.password = password;
    this.user_id = user_id;
  }
}

export default SQL_USER;
